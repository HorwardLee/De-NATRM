# Package Parity Check

## Final Source

Source release lineage:

`de_natrm_release_candidate_pr90_export`

The package copies the final release assets without retraining:

- `causal_net.pt`
- `preprocessor.joblib`
- `data_meta.json`
- `treatment_map.json`
- `treatments_keep.json`
- final metrics and test prediction CSV reports

## Identity Parity

The package identity is pinned in `FINAL_MODEL_ID.json`:

- Model ID: `de_natrm_final_release_pr90_20260423`
- Final configuration: causal + 18 primary ROI + 7 composite ROI + 6 PET shape ROI
- Threshold strategy: `precision_at_recall`
- Selected threshold: `0.4630236625671386`
- Test metrics: AUROC 0.8693, AUPRC 0.9076, Brier 0.1946, Precision 0.8649, Recall 0.9412

## Hash Parity

`manifest.json` records SHA256 hashes for all copied artifacts. `scripts/validate_package.py` verifies those hashes against the package files.

Validation run on 2026-07-02:

```bash
python3 export/model_package/scripts/validate_package.py \
  --output export/model_package/reports/validation_report.json
```

Result: all checks passed.

- `model_id_ok`: true
- `threshold_ok`: true
- `num_feature_count_ok`: true
- `roi_branch_dim_ok`: true
- `treatments_ok`: true
- `hashes_ok`: true

## Inference Smoke Check

`examples/example_prediction_output.json` was generated by:

```bash
python3 export/model_package/scripts/predict_package.py \
  --input export/model_package/examples/example_patient_input.json \
  --output export/model_package/examples/example_prediction_output.json
```

The example patient is intentionally minimal and marks ROI as unavailable, so ROI branch gating is expected.

## Known Constraint

Full row-by-row parity against raw held-out patients requires the original raw patient feature table and split reconstruction. This package therefore freezes and verifies the source test probability report by hash, and validates that the copied model/preprocessor can be loaded and used for independent one-patient inference.
